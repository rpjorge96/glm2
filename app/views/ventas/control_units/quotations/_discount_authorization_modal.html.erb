<div
  data-controller="quotation--discount-authorization"
  data-action="quotation:openDiscountAuthorizationModal@window->quotation--discount-authorization#openModal"
  id="discountAuthorizationModal"
  tabindex="-1"
  aria-modal="true"
  role="dialog"
  class="hidden overflow-y-auto overflow-x-hidden fixed top-0 right-0 left-0 z-50 justify-center items-center w-full md:inset-0 h-[calc(100%-1rem)] max-h-full"
>
  <div class="relative max-h-full w-full max-w-2xl">
    <!-- Modal content -->
    <div class="relative rounded-lg bg-white shadow dark:bg-gray-700">
      <!-- Modal header -->
      <div class="flex items-start justify-between rounded-t border-b p-5 dark:border-gray-600">
        <h3 class="text-xl font-semibold text-gray-900 dark:text-white lg:text-2xl">
          Solicitud de Autorización de Descuento
        </h3>
      </div>
      <!-- Modal body -->
      <div class="space-y-6 p-6">
        <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
          <span class="text-gray-700 dark:text-gray-400">
            Esta excediendo el monto maximo aprobado para descuento (<span data-quotation--discount-authorization-target="maxAllowedDiscount"> </span>). Desea realizar una solicitud para dar mas descuento?
          </span>
          <textarea
            class="w-full h-24 p-2 mt-2 border border-gray-300 rounded-lg focus:ring-4 focus:ring-blue-300 dark:border-gray-600 dark:focus:ring-blue-800"
            placeholder="Comentarios"
            data-quotation--discount-authorization-target="comment"
          ></textarea>

        </div>
      </div>
      <div
        class="flex justify-between space-x-2 rtl:space-x-reverse rounded-b border-t border-gray-200 p-6 dark:border-gray-600"
      >
        <button
          type="button"
          class="rounded-lg bg-red-700 px-5 py-2.5 text-center text-sm font-medium text-white hover:bg-red-800 focus:outline-none focus:ring-4 focus:ring-red-300 dark:bg-red-600 dark:hover:bg-red-700 dark:focus:ring-red-800"
          data-action="click->quotation--discount-authorization#closeModal"
        >
          Cancelar
        </button>
        <button
          type="button"
          class="rounded-lg bg-blue-700 px-5 py-2.5 text-center text-sm font-medium text-white hover:bg-blue-800 focus:outline-none focus:ring-4 focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
          data-action="click->quotation--discount-authorization#sendComment"
        >
          Enviar
        </button>
      </div>
    </div>
  </div>
</div>

