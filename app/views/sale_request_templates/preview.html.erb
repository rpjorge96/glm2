<div class="header">
  <%= render(Pdf::ImageComponent.new(url: asset_url("pdf/logo.png"), height: "50px")) %>
  <h1>Solicitud De Compra</h1>
  <%= render(Pdf::ImageComponent.new(url: asset_url("pdf/logo.png"), height: "50px")) %>
</div>

<div class="reference-number">
  <h2>PLB-00001</h2>
</div>

<div class="preview-body">
  <%= raw SaleRequestText.new(@sale_request_template.body, preview: true).call %>
</div>

<style>
  .header {
      display: flex;
      justify-content: space-around;
      font-size: 35px;
      font-weight: 700;
      text-align: center;
      padding-top: 1rem;
  }

  .preview-body {
      padding: 0.1rem 1.8rem;
  }

  .reference-number {
      color: red;
      font-size: 25px;
      font-weight: 700;
      text-align: right;
      padding: 0.5rem 1.8rem;
  }
</style>