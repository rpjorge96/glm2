<%= image_tag image_url_path, style: "width: #{@width || 'auto'}; height: #{@height || 'auto'};", class: @custom_class, loading: "eager" %>

<style>
  .extra-image{
      max-height: inherit;
  }
</style>
